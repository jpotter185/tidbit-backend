services:
  tidbit:
    build: .
    container_name: tidbit-backend
    expose:
      - "6769"
    environment:
      - PORT=6769
      - API_KEY=${API_KEY}
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    external: true